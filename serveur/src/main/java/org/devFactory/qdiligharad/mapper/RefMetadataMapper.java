package org.devFactory.qdiligharad.mapper;

import org.devFactory.qdiligharad.domain.RefCategoryService;
import org.devFactory.qdiligharad.domain.RefMetadata;
import org.devFactory.qdiligharad.dto.RefCategoryServiceDTO;
import org.devFactory.qdiligharad.dto.RefMetadataDTO;
import org.springframework.stereotype.Component;

@Component
public class RefMetadataMapper extends AbstractMapper<RefMetadata, RefMetadataDTO> {
}
