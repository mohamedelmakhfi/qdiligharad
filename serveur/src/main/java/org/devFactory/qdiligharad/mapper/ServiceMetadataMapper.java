package org.devFactory.qdiligharad.mapper;

import org.devFactory.qdiligharad.domain.Role;
import org.devFactory.qdiligharad.domain.ServiceMetadata;
import org.devFactory.qdiligharad.dto.RoleDTO;
import org.devFactory.qdiligharad.dto.ServiceMetadataDTO;
import org.springframework.stereotype.Component;

@Component
public class ServiceMetadataMapper extends AbstractMapper<ServiceMetadata, ServiceMetadataDTO> {
}
