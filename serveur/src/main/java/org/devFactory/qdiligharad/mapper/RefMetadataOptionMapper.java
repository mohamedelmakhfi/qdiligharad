package org.devFactory.qdiligharad.mapper;

import org.devFactory.qdiligharad.domain.RefMetadataOption;
import org.devFactory.qdiligharad.dto.RefMetadataOptionDTO;
import org.springframework.stereotype.Component;

@Component
public class RefMetadataOptionMapper extends AbstractMapper<RefMetadataOption, RefMetadataOptionDTO> {
}